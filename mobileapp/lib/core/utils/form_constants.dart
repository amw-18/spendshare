// lib/core/utils/form_constants.dart
import 'package:flutter/material.dart';

// Standard vertical spacing between form elements
const double formVerticalSpacing = 20.0;

// Standard horizontal padding for form containers
const EdgeInsets formHorizontalPadding = EdgeInsets.symmetric(horizontal: 24.0);

// Standard padding for form fields
const EdgeInsets formFieldPadding = EdgeInsets.symmetric(vertical: 10.0);

// Standard button height
const double formButtonHeight = 50.0;

// Standard button padding
const EdgeInsets formButtonPadding = EdgeInsets.symmetric(vertical: 12.0, horizontal: 24.0);

// Standard input border radius
final BorderRadius formInputBorderRadius = BorderRadius.circular(8.0);
